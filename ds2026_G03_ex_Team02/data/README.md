# data/

本书插图引用的**小型结果表**可放在此目录；完整数据集请使用上级目录：

- `../../data/` — 原始下载
- `../../clean/` — 清洗后面板
- `../../output/` — 回归 CSV 与脚本原始 PNG
- `../images/` — 本书插图（由 `scripts/sync_images.ps1` 从 `output/` 同步，部署时请提交）

渲染前无需复制全量数据；仅在章节中需要 `read_csv` 演示时再添加样本文件。
