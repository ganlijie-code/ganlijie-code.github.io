# 插图目录

本书所有章节引用的 PNG 均放在此目录，便于将 `ds2026_G03_ex_Team02/` 作为**独立仓库**部署到 GitHub Pages（无需依赖 `Marp/` 或上级 `output/`）。

## 从 DS 分析流水线同步

在 `DS` 根目录已运行 `scripts/analysis.py` 与 `advanced_analysis.py` 后：

```powershell
.\ds2026_G03_ex_Team02\scripts\sync_images.ps1
```

## 章节内引用

相对 `chapters/*.qmd`：`../images/fig1_cxkj_revenue_profit.png`
